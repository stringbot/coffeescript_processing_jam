var x_siner = document.GetSiner();
var y_siner = document.GetSiner();

void setup() {
  size(640,480);
  stroke(100);
  strokeWeight(2);
}

void draw() {
  int x = x_siner();
  int y = y_siner();
  
  background(0);
  ellipse(320+x,240+y,10,10);
}