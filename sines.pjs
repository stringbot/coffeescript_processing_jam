var siner = document.GetSiner(640,480);

void setup() {
  size(640,480);
  stroke(100);
  strokeWeight(2);
}

void draw() {
  var coords = siner();
  int x = coords[0];
  int y = coords[1];
  
  background(200);
  ellipse(x,y,10,10);
}